# exp05_ablate_shift_family

Ablation: replace Gaussian mean-shift with (i) quantile slice on feature 0
and (ii) importance resampling on feature 0. Same models/seeds as a reduced
H1 grid (binary datasets only — quantile slice can drop a class on wine).
